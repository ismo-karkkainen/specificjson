writeBool:
  writer: true
  declaration: |
    template<typename Sink>
    void Write(Sink& S, bool Value, std::vector<char>& Buffer);
  header: write_Bool.hpp
  license: ../LICENSE.txt
  includes:
  - "#include <vector>"
