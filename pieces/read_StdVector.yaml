StdVector:
  description: Parses an array of scalars into std::vector.
  header: read_StdVector.hpp
  license: ../License.txt
  requires:
    - ParseArrayContainer
  includes:
    - "#include <vector>"
