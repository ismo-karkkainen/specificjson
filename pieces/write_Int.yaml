writeInt:
  writer: true
  declaration: |
    template<typename Sink>
    void Write(Sink& S, int Value, std::vector<char>& Buffer);
  header: write_Int.hpp
  source: write_Int.cpp
  license: ../License.txt
  requires: writeScalarType
  includes:
  - "#include <vector>"
